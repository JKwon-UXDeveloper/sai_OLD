{
    "homepage": "https://No1UXDeveloper.github.io/sai",
    "name": "sai",
    "version": "0.1.0",
    "scripts": {
        "predeploy": "npm run build",
        "deploy": "gh-pages -d build",
        "start": "react-scripts start",
        "build": "react-scripts build",
        "test": "react-scripts test",
        "eject": "react-scripts eject",
        "client": "cd client && yarn start",
        "server": "nodemon server.js",
        "dev": "concurrently --kill-others-on-fail \"   yarn server     \" \"   yarn client     \"  ",
        "github-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
    },
    "dependencies": {
        "@material-ui/core": "^4.12.3",
        "@material-ui/icons": "^4.11.2",
        "axios": "^0.26.0",
        "body-parser": "^1.18.3",
        "concurrently": "^7.0.0",
        "express": "^4.16.4",
        "multer": "^1.4.4",
        "mysql": "^2.18.1",
        "react-scripts": "5.0.0"
    },
    "devDependencies": {
        "gh-pages": "^3.2.3"
    }
}
